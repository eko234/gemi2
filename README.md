# gemi2
This is an agentic assistant tool for the kakoune text editor, it requires you to spin up the server locally tho, and also to set the GEMINI_API_KEY env var

# default tools
there is a TODOS default tool, for it to work properly you need to provide the TODOS_PATH env var
